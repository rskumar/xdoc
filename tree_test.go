package xdoc

import "testing"

func Test_TreeSimple2(t *testing.T) {
	/*	nRoot := NewNode(NewPage())
		title := NewNode("title")
		para := NewNode("para")
		para.AppendChild(NewNode("a"))
		err := nRoot.AppendChild(title, para);
		if err != nil {
			t.Error(err)
		}
		t.Log(NodeTreeAsString(nRoot))*/
}
